// 계산 기능하는 하는 파일

const add = (a, b) => a + b;
const subtract = (a, b) => a - b;

// module.exports = {
//   moduleName : 'calculator', 
//   add : add,
//   subtract:subtract
//  };

module.exports = {add, subtract};